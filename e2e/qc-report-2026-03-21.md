# QC Report: Full Demo Flow — Live Bidding Test
**Scope**: Full auction lifecycle (create item → admin approve → create auction → deposit → bid) | **Date**: 2026-03-21

## Test Summary

Tested the complete demo flow with 4 actors (admin, seller, bidder1, bidder2) using 5 Playwright MCP browser instances against the live VPS API (`api.newlsun.com`).

**Result: Deposit and bidding flows could NOT be tested** due to FE bugs blocking the qualification/deposit UI. The item creation, admin review, and auction creation flows were tested end-to-end.

---

## Automated Results

| # | Check | Status | Details |
|---|-------|--------|---------|
| 1 | Build | SKIP | Per stakeholder — focus on E2E only |
| 2 | Lint | SKIP | Per stakeholder |
| 3 | Login (all 4 actors) | PASS | admin, seller, bidder1, bidder2 all logged in |
| 4 | Create Item + multi-upload | PASS | 3 items created with images, multi-upload works |
| 5 | Submit for Review | PASS | All 3 items submitted (draft → pending_review) |
| 6 | Admin Approve | PASS | All 3 items approved via admin review queue |
| 7 | Create Auction (3-step) | PARTIAL | Step order works (create→submit→timing) but token expiry causes silent failures |
| 8 | Publish Auction | PARTIAL | A1 published OK; A2, A3 fail with BE error |
| 9 | Deposit/Qualification | BLOCKED | No deposit button shown (FE bug) |
| 10 | Bidding | BLOCKED | Requires qualified bidder (blocked by #9) |
| 11 | Buy-Now | BLOCKED | A2 not published (BE bug) |
| 12 | Sealed Bid | BLOCKED | A3 not published (BE bug) |

---

## Bugs Found

### FE Bugs

| # | Severity | Description | File | Root Cause | Fix Suggestion |
|---|----------|-------------|------|------------|----------------|
| **#1** | LOW | My Listings shows Items(0) on first navigation after login | `auctionService.ts:649` | `getMyItems()` catch returns `[]` instead of throwing; TanStack Query caches the empty result | Remove `try/catch` or re-throw non-404 errors so TanStack Query can retry |
| **#2** | MEDIUM (FIXED) | Date picker disables today's date | `CreateAuctionPage.tsx:199` | `disablePassedDates` used `'minute'` granularity which disables entire current day | Changed to `'day'` granularity — **fixed during session** |
| **#3** | HIGH | `getMyItems()` silently swallows 401 errors on token expiry | `auctionService.ts:657` | `catch { return []; }` catches ALL errors including 401s that axios interceptor would retry. TanStack Query caches `[]` permanently | Remove catch or only catch 404. Let TanStack Query handle retries via `retry` config |
| **#5** | HIGH | Auction detail page renders empty on first load after token expiry | `auctionService.ts:349` | Same root cause as #3 — `getAuctionById` catch returns `null`, TanStack Query caches it | Same fix — let errors propagate so TanStack Query retries |
| **#7** | HIGH (BLOCKER) | No deposit/qualification button shown during qualification window | `BiddingPanel.tsx:262` | `QualificationSection` gated behind `isActive` but deposits happen during `scheduled` status. Also `VITE_BYPASS_DEPOSIT` defaults to `true`, hiding the section even for active auctions | 1. Show QualificationSection for `scheduled` status when within qual window. 2. Set `VITE_BYPASS_DEPOSIT=false` or remove the bypass |

### BE Bugs (for Tân)

| # | Severity | Description | Endpoint | Evidence | Cross-check |
|---|----------|-------------|----------|----------|-------------|
| ~~**#4**~~ | ~~MEDIUM~~ | ~~Submit returns 200 instead of 401~~ | — | — | **NOT A BUG** — FE axios interceptor correctly caught 401, refreshed token, replayed request. The "200" was the refresh call. BE auth is correctly implemented (`CustomJwtBearerEvents.Challenge()` returns 401 for `SecurityTokenExpiredException`). |
| **#6** | HIGH | Publish fails with "timing must be set" on already-scheduled auctions | `POST /api/auctions/{id}/publish` | A1 published fine, A2/A3 fail with 400. All 3 in `scheduled` status with timing set. | **CONFIRMED BE BUG** — Race condition. `AuctionInfo` is a `ComplexProperty` (EF Core) stored in `auctions` table columns. Domain declares `AuctionInfo?` (nullable) but `ComplexProperty` doesn't support null — mismatch. When timing write for A2/A3 hasn't fully committed and publish reads stale data, `Info` appears null. **Fix**: Add logging in `PublishAuctionCommandHandler`, consider `AsNoTracking()` reload, investigate nullable `ComplexProperty` mismatch. Source: `Auction.cs:315-324`, `AuctionConfiguration.cs:96`. |
| **BE-CORS** | LOW/MEDIUM | SignalR CORS — **partially fixed by Tân**. `localhost:3000` now works (verified). VPS origin `http://14.225.222.182` still not whitelisted. | SignalR Hub | Negotiate returns 200 + WebSocket transport for localhost. CORS preflight from VPS origin returns no headers. |

---

## Test Auctions Created

| Auction | ID | Type | Status | Timing (UTC) |
|---------|-----|------|--------|-------------|
| A1 Regular | `019d0f23-6559-7215-b6cc-5e7f55fefdbf` | regular | scheduled (published) | Start 07:25, End 07:35 |
| A2 Buy Now | `019d0f25-067f-7b24-ae13-13f0e42337a3` | regular + buyNow 1M | scheduled (NOT published — BE bug) | Start ~07:26, End ~07:36 |
| A3 Sealed | `019d0f25-4e44-714f-89cd-b80406bfd8f1` | sealed | scheduled (NOT published — BE bug) | Start ~07:26, End ~07:36 |

---

## Steps Completed Successfully

1. **Login** — All 4 actors logged in ✓
2. **Create 3 items** — Multi-image upload works (2 images on item 1) ✓
3. **Submit for review** — All 3 items moved to `pending_review` ✓
4. **Admin approve** — All 3 items approved via review queue ✓
5. **Create auctions** — 3-step flow (create→submit→timing) works when token is fresh ✓
6. **Publish** — A1 published successfully ✓
7. **Auction detail** — Renders correctly for scheduled auction (after page reload) ✓

## Steps Blocked

8. **Deposit** — No UI button to deposit (Bug #7)
9. **Manual bidding** — Requires qualified bidder
10. **Outbid scenario** — Requires 2 qualified bidders
11. **Buy-now** — A2 not published (BE Bug #6)
12. **Sealed bid** — A3 not published (BE Bug #6)
13. **Auto-extension** — Requires active auction with bids
14. **Auction end verification** — Requires completed auction

---

## Manual Tests Required

- [ ] VNPay deposit flow → bidder redirected to VNPay, pays, returns qualified — **Why manual**: MCP cannot interact with VNPay external redirect (category 2)
- [ ] SignalR real-time bid updates → bid placed by bidder1 appears live on bidder2's screen — **Why manual**: SignalR blocked by BE CORS (category 3)
- [ ] Visual layout on mobile devices — **Why manual**: Requires DevTools device emulation (category 4)

---

## Priority Fix Order

1. **Bug #7** (BLOCKER) — Fix QualificationSection visibility for scheduled auctions + disable BYPASS_DEPOSIT
2. **Bug #3/#5** (HIGH) — Fix silent error swallowing in service functions — let TanStack Query retry
3. **Bug #6** (BE — HIGH) — Tân: Race condition in publish — `AuctionInfo` ComplexProperty read before timing commit. See `Auction.cs:315-324`, `AuctionConfiguration.cs:96`
4. **Bug #1** (LOW) — Same root cause as #3, will be fixed together

~~Bug #4~~ — Not a bug. FE axios interceptor working as designed.

---

## Verdict: FAIL

**Reason**: Deposit and bidding flows are blocked by FE Bug #7 (no deposit button) and BE Bug #6 (publish fails). These are the core demo features that cannot be tested.

**To unblock**: Fix Bug #7 (show QualificationSection for scheduled auctions, disable BYPASS_DEPOSIT), then re-test the full deposit → bid → outbid → auto-extension flow.
